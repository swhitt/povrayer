# Generated build output (emscripten + tsc)
dist/
# Compiled web/ TypeScript (tools/build-web.mjs); tsc's own formatting, not ours
_build/
# Gitignored symlink to the built wrapper (tools/link-wrapper.mjs)
src/index.js
# Vendored third-party (MIT, pinned)
web/coi-serviceworker.js
# Generated coverage reports
coverage/
# Dependencies and scratch
node_modules/
.playwright-cli/
# Agent worktrees: full checkouts of this repo nested inside it, so every ignore
# rule below would otherwise need a second, path-prefixed copy to match. Only the
# transient worktrees are skipped, not tracked .claude/ project config.
.claude/worktrees/
# Lockfiles (npm owns their formatting)
package-lock.json
wrapper/package-lock.json
# Scene fixtures / SDL are not JS-formattable
test/fixtures/
# Generated autocomplete data (tools/includes-manifest/generate.mjs); one symbol
# per line for diffability, which prettier would explode across five lines each
web/includes-manifest.json
